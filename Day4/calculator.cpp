#include<bits/stdc++.h>
using namespace std;

int main()
{
    int a, b;
    int sum;
    cout << "Type a number: ";
    cin >> a; // usr input a
    cout << "Type another number: ";
    cin >> b; // user input b
    sum = a + b; // sum of a and b
    cout << "Sum of " << a << " and " << b << " is: " << sum << endl;
    return 0;
}