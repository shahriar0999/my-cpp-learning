#include <bits/stdc++.h>
using namespace std;


int main(){
    int a, b; cin >> a >> b;
    cout << (a == b) << endl;   // equal equal
    cout << (a != b) << endl; // not equal
    cout << (a >= b) << endl; // greater than eqal
    cout << (a <= b) << endl; // less than equal
}